# 独角数卡 Docker 部署配置

# Docker镜像版本 (可选: latest, 2.1.0-build.X, 2.1.0等)
DOCKER_TAG=latest

# 应用配置
APP_URL=http://localhost:8080
APP_KEY=base64:your_app_key_here

# 数据库配置
DB_PASSWORD=dujiaoka123
MYSQL_ROOT_PASSWORD=root123

# 可选：自定义端口
# WEB_PORT=8080
# MYSQL_PORT=3306
# REDIS_PORT=6379

# 开发模式：是否挂载源代码到 /opt/dujiaoka (true/false)
# 设为 true 时需要先运行 docker-init.sh 初始化文件
MOUNT_SOURCE_CODE=false
# REDIS_PORT=6379